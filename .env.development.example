# 前端本地开发环境配置模板 (.env.development.example)
# ====================================================
# 重要：请将此文件复制为 `.env.development` 并填写实际值
# 此文件包含前端应用本地开发所需的环境变量
# 请根据您的开发环境选择合适的 API 端点

# =================== 安全注意事项 ===================
# 前端配置主要包含 API 端点和实例标识，不包含敏感凭据。
# 但以下安全实践仍需注意：
# 1. API 端点：确保使用正确的协议（http/https, ws/wss）
#    - 开发环境可使用 HTTP/WS
#    - 生产环境必须使用 HTTPS/WSS
# 2. 实例标识：用于区分不同部署实例
#    - 避免使用敏感信息作为实例名
#    - 生产环境使用有意义的标识（如 region, version）
# 3. 环境变量：前端环境变量会暴露在浏览器中
#    - 不要在前端配置中放置敏感信息（密钥、密码等）
#    - 敏感操作应通过后端 API 进行
# 4. CORS 配置：确保后端正确配置 CORS，防止跨域攻击
# ====================================================

# API 端点配置
# ------------
# 后端 API 基础地址
# 本地开发：使用 http://localhost:3001/v1
# Docker Compose 开发：使用 http://booking-backend:3001/v1
# 生产环境：使用实际的 API 域名
NEXT_PUBLIC_API_URL=http://localhost:3001/v1

# WebSocket 连接地址
# 本地开发：使用 ws://localhost:3001/ws
# Docker Compose 开发：使用 ws://booking-backend:3001/ws
# 生产环境：使用 wss:// 协议和实际域名
NEXT_PUBLIC_WS_URL=ws://localhost:3001/ws

# 实例标识
# --------
# 用于区分不同前端实例（在多实例部署时）
# 本地开发建议使用 "dev" 或 "local"
# 历史遗留：之前使用 instance1, instance2, instance3
NEXT_PUBLIC_INSTANCE_NAME=dev

# 可选配置（如需）
# --------------
# 调试模式：启用额外的日志输出
# NEXT_PUBLIC_DEBUG=true

# 功能开关：控制特定功能是否启用
# NEXT_PUBLIC_FEATURE_FLAG_NEW_UI=false

# 配置说明：
# ---------
# 1. 本地开发（直接运行前端和后端）：
#    - NEXT_PUBLIC_API_URL: http://localhost:3001/v1
#    - NEXT_PUBLIC_WS_URL: ws://localhost:3001/ws
#    - NEXT_PUBLIC_INSTANCE_NAME: dev
#
# 2. Docker Compose 开发（使用 docker-compose up）：
#    - NEXT_PUBLIC_API_URL: http://booking-backend:3001/v1
#    - NEXT_PUBLIC_WS_URL: ws://booking-backend:3001/ws
#    - NEXT_PUBLIC_INSTANCE_NAME: dev
#
# 3. 生产环境：
#    - NEXT_PUBLIC_API_URL: https://api.yourdomain.com/v1
#    - NEXT_PUBLIC_WS_URL: wss://api.yourdomain.com/ws
#    - NEXT_PUBLIC_INSTANCE_NAME: 根据实际实例配置
#
# 注意事项：
# - 确保后端服务与配置的 API 地址匹配
# - WebSocket 地址协议与 HTTP 地址协议一致（http -> ws, https -> wss）
# - 实例名用于日志和监控，不影响核心功能